# Phase 5 — Main app rebuild

**Status:** Pending

## Goal

User product on Vite + React 19 + Tailwind 4 + React Router 7.

## Steps

1. Tailwind 4 + theme tokens  
2. Router, store, theme provider, auth gate  
3. Screens: landing/auth → dashboard → workspace → space → board → notifications → chat → settings → invites → AI → billing  
4. RTK + RTK Query for server data  
5. Typed socket hooks  

## Exit checks

- [ ] Login → dashboard → board works
- [ ] Create/update/move task persists + sockets
- [ ] Production `vite build` succeeds
- [ ] No mobile API references

## Completion notes

*(Fill when this phase finishes.)*
