# Phase 7 — Cross-cutting quality

**Status:** Pending

## Goal

Security, tests, CI, docs, reliable seed path.

## Steps

1. Helmet, CORS allowlist, rate limiting, upload validation  
2. Structured logging  
3. Backend + frontend + socket tests for critical paths  
4. CI: lint, type-check, test, build on Node 22  
5. Docs / API overview  
6. One reliable local seed  

## Exit checks

- [ ] CI green
- [ ] `npm run build` builds packages + main + admin
- [ ] Fresh clone → env → seed → `dev:web` under 30 minutes

## Completion notes

*(Fill when this phase finishes.)*
