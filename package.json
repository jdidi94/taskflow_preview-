{
  "name": "taskflow-ai",
  "version": "3.0.0",
  "description": "TaskFlow AI Smart Team Task Manager (v3)",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "concurrently -k -p \"[{name}]\" -n \"Backend,Main,Admin\" -c \"red,blue,green\" \"npm run dev:backend\" \"npm run dev:main\" \"npm run dev:admin\"",
    "dev:backend": "npm run dev -w @taskflow/backend",
    "dev:main": "npm run dev -w @taskflow/main",
    "dev:admin": "npm run dev -w @taskflow/admin",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "show:ports": "echo \"Backend: http://localhost:3001\" && echo \"Main App: http://localhost:5173\" && echo \"Admin Panel: http://localhost:5175\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.5.3",
    "turbo": "^2.5.0",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.0.0"
}
